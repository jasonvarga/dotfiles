This repo contains dotfiles (configuration, etc) for various utilities and applications.
The sources of truth are in this repo, and symlinks are set up to the appropriate locations.

For Claude specifically, some files in `~/.claude` exist in this repo's `claude` subdirectory.

Clade statusline docs are here: https://code.claude.com/docs/en/statusline